language: c
install: sudo apt-get install -y apache2-utils

jobs:
  include:
  - stage: Build java
    os: linux
    jdk: oraclejdk8
    script:
      - mvn clean install
  - stage: Build plain C
    os: linux
    script:
      - make clean; make






